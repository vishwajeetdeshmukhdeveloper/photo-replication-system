services:
  - type: web
    name: photo-replication-system
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    plan: free # Change this to a paid plan if you need more resources
